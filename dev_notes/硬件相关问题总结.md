## Maleoon GPU Hardware Spec
1. 支持OpenCL 3.0
2. 硬件规格 xxx
3. 我的当前GPU软件版本支持sub_group_shuffle_down指令。

## 编译器相关：
### ARM和x86的实现差异
ARM NEON的FMA指令将a*b+c合并为一次操作只舍入一次，而x86 SSE的mul 和 add 分别舍入，
需要设置-ffp-contract=off编译选项禁止FMA合并，确保两平台舍入行为一致。